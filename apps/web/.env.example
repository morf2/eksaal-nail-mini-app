VITE_API_URL=http://localhost:4000
VITE_TELEGRAM_BOT_USERNAME=eksaal_nail_bot

# Admin login/password are no longer configured here — verified server-side only,
# see apps/api/.env.example (ADMIN_LOGIN / ADMIN_PASSWORD_HASH).
