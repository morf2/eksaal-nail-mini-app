TELEGRAM_BOT_TOKEN=
WEBHOOK_URL=
API_BASE_URL=http://localhost:4000
# The master's own Telegram chat id — the only chat that gets promoted to the
# MASTER role (and therefore receives new-booking notifications) when it sends
# /start. Get it by messaging @userinfobot on Telegram, or checking the "chatId"
# your own /start logs before this is set.
MASTER_TELEGRAM_CHAT_ID=
